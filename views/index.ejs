<%- include('./partials/header.ejs') %>
<%- include('./partials/home-navbar.ejs') %>

<body>
    <div class="container">
        <hr>
            <div class="bg-light p-5 rounded-lg m-3">
            <h1 class="display-4">Bem vindo!</h1>
            <p class="lead">Blog GestaoREAL Tecnologia</p>
            <hr class="my-4">
            <p>Falamos das tecnologias mais disruptivas do mercado.</p>
            <a class="btn btn-secondary btn-sm" href="#" role="button">Saiba mais</a>
            </div>
        <hr>
        <% articles.forEach(article => { %>
            <div class="card">
                <div class="card-header">
                    <h2><%= article.title %></h2>
                </div>
                <div class="card-body">
                    <a href="<%= article.slug %>" class="btn btn-success">Ler artigo</a>
                </div>
            </div>
            <br>
        <% }) %>
    </div>
</body>

<%- include('./partials/footer.ejs') %>